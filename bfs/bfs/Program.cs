using System;
using System.Collections;
using System.Collections.Generic;

namespace bfs
{
    class Program
    {
        static void Main(string[] args)
        {
            /*
            Bfs bfs = new Bfs();

            bfs.DictionaryGraph.Add("you", new List<string> { "alice", "bob", "claire"});
            List<string> lst1 = new List<string> { "dad", "bob" };
            List<string> lst2 = new List<string> { "alice", "kent" };
            bfs.DictionaryGraph.Add("alice", lst1);
            bfs.DictionaryGraph.Add("bob", lst2);

            bfs.Bfs_Search("you");
            */
            /* 
            foreach(KeyValuePair<string, List<string>> kvp in bfs.Graph)
            {
                Console.Write("Key = {0} values = ", kvp.Key);
                foreach(string value in kvp.Value)
                    Console.Write("{0} ", value);
                Console.WriteLine();
            }
            */


        }
    }
}
